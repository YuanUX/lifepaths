                      <div>
                         <label className="block text-xs font-bold text-slate-500 mb-1.5 uppercase">Notes</label>
                         <textarea 
                           className="w-full border border-slate-300 rounded-md p-2 text-sm focus:ring-2 focus:ring-indigo-500 outline-none resize-none" 
                           rows={4}
                           placeholder="Add notes about this goal..."
                           value={goal.notes || ''} 
                           onChange={async (e) => {
                             const updated = {...goal, notes: e.target.value};
                             setData({...data, goals: data.goals.map(g => g.id === goal.id ? updated : g)});
                             if (!isDemoMode) await DataService.updateGoal(updated);
                           }}
                         />
                      </div>
